# 🚀 ІНСТРУКЦІЯ ДЛЯ ЗАПУСКУ REACT ПРОЕКТУ

## ⚡ ШВИДКИЙ СТАРТ (2 ХВИЛИНИ)

### Крок 1: Перейдіть у папку проекту
```bash
cd drone-manufacturer
```

### Крок 2: Встановіть залежності
```bash
npm install
```
*Це завантажить усі необхідні пакети (~200MB)*

### Крок 3: Запустіть dev сервер
```bash
npm run dev
```

### Крок 4: Відкрийте браузер
```
http://localhost:3000
```

**Готово! 🎉**

---

## 📋 ВИМОГИ

- **Node.js** 16+ ([завантажити](https://nodejs.org/))
- **npm** (йде з Node.js)
- **Браузер** Chrome, Firefox, Safari або Edge

### Перевірка версії:
```bash
node --version    # Повинно бути v16.0.0 або вище
npm --version     # Повинно бути 8.0.0 або вище
```

---

## 🎯 ОСНОВНІ КОМАНДИ

| Команда | Опис |
|---------|------|
| `npm install` | Встановити залежності |
| `npm run dev` | Запустити dev сервер |
| `npm run build` | Сборити для продакшну |
| `npm run preview` | Переглянути продакшн версію |

---

## 📁 СТРУКТУРА ПРОЕКТУ

```
drone-manufacturer/
├── src/
│   ├── components/          # React компоненти
│   │   ├── Header.tsx
│   │   ├── Hero.tsx
│   │   ├── Products.tsx
│   │   ├── Solutions.tsx
│   │   ├── Specs.tsx
│   │   ├── Testimonials.tsx
│   │   ├── Contact.tsx
│   │   ├── Footer.tsx
│   │   └── Solution.tsx
│   ├── App.tsx              # Головний компонент
│   ├── main.tsx             # Точка входу
│   └── index.css            # Стилі
├── index.html               # HTML шаблон
├── package.json             # Залежності
├── tailwind.config.js       # Конфіг Tailwind
├── vite.config.ts           # Конфіг Vite
└── tsconfig.json            # Конфіг TypeScript
```

---

## 🔧 ВСТАНОВЛЕННЯ ЗАЛЕЖНОСТЕЙ

### Якщо npm install не працює:

**Спосіб 1: Очистити кэш**
```bash
npm cache clean --force
npm install
```

**Спосіб 2: Видалити та переустановити**
```bash
rm -rf node_modules package-lock.json
npm install
```

**Спосіб 3: Використати yarn**
```bash
npm install -g yarn
yarn install
yarn dev
```

---

## 🖥️ ЗАПУСК DEV СЕРВЕРА

```bash
npm run dev
```

Ви побачите щось таке:
```
  VITE v5.0.0  ready in 234 ms

  ➜  Local:   http://localhost:3000/
  ➜  press h to show help
```

### Якщо порт 3000 занятий:

**Спосіб 1: Змінити номер порту**
Отредактуйте `vite.config.ts`:
```typescript
server: {
  port: 3001  // Змініть на інший номер
}
```

**Спосіб 2: Закрити програму на порту 3000**
```bash
# На Windows (PowerShell):
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# На macOS/Linux:
lsof -i :3000
kill -9 <PID>
```

---

## 🌍 ЯК РЕДАГУВАТИ КОД

### Редагування в реальному часі:
1. Отредактуйте будь-який файл в `src/`
2. Збережіть файл (Ctrl+S)
3. Браузер автоматично оновиться

### Приклад: Зміна заголовка
**Файл:** `src/components/Hero.tsx`
```typescript
// Знайдіть:
<h1 className="text-5xl md:text-6xl font-bold leading-tight...">
  Майбутнього неба сьогодні
</h1>

// Замініть на:
<h1 className="text-5xl md:text-6xl font-bold leading-tight...">
  Ваш заголовок тут
</h1>
```

---

## 🎨 РЕДАГУВАННЯ КОЛЬОРІВ

**Файл:** `tailwind.config.js`

Знайдіть секцію `colors`:
```javascript
colors: {
  'sky-dark': '#0a0e27',    // Фон
  'sky-blue': '#0066ff',    // Основний
  'sky-light': '#f5f7fa',   // Світлий
  'sky-accent': '#00d4ff',  // Акцент
}
```

Замініть на ваші кольори:
```javascript
colors: {
  'sky-dark': '#1a1a2e',    // Новий колір фону
  'sky-blue': '#ff0000',    // Новий основний
  'sky-light': '#ffffff',   // Новий світлий
  'sky-accent': '#ffff00',  // Новий акцент
}
```

---

## 📱 ТЕСТУВАННЯ НА МОБІЛЬНИХ

### У браузері (F12):
1. Натисніть F12 (Dev Tools)
2. Натисніть Ctrl+Shift+M (Toggle Device Toolbar)
3. Виберіть пристрій (iPhone, iPad, Android)

### На реальному девайсі:
1. Дізнайтеся IP вашого комп'ютера:
```bash
ipconfig        # Windows
ifconfig        # macOS/Linux
```

2. Відкрийте на телефоні:
```
http://192.168.X.X:3000
```
*(замініть X на вашу IP)*

---

## 🔨 СБОРКА ДЛЯ ПРОДАКШНУ

```bash
npm run build
```

Це створить папку `dist/` з оптимізованим кодом.

### Розміри файлів:
```
dist/index.html        ~2KB
dist/assets/*.js       ~50KB
dist/assets/*.css      ~5KB
```

### Развертування:
1. Скопіюйте вміст папки `dist/`
2. Загрузьте на хостинг (Vercel, Netlify, GitHub Pages)

---

## 🐛 РІШЕННЯ ПРОБЛЕМ

### ❌ "Cannot find module 'react'"
```bash
rm -rf node_modules
npm install
```

### ❌ "Port 3000 already in use"
Змініть порт у `vite.config.ts` на 3001

### ❌ "npm command not found"
Переустановіть Node.js з [nodejs.org](https://nodejs.org/)

### ❌ "Styles not loading"
```bash
# Перезавантажте сервер (Ctrl+C, потім npm run dev)
# Очистите кэш браузера (Ctrl+Shift+Delete)
# Перезавантажте сторінку (Ctrl+Shift+R)
```

### ❌ "TypeScript errors"
Це не проблема - просто попередження. Проект все одно буде працювати.

---

## 📊 РОЗРОБКА

### Структура компонента:
```typescript
import React from 'react'
import { Icon } from 'lucide-react'

export const MyComponent: React.FC = () => {
  return (
    <section className="py-24 px-4">
      <div className="max-w-7xl mx-auto">
        <h2 className="text-4xl font-bold">
          Привіт!
        </h2>
      </div>
    </section>
  )
}
```

### Додавання нового компонента:
1. Створіть файл: `src/components/MyComponent.tsx`
2. Напишіть код компонента
3. Імпортуйте в `src/App.tsx`:
```typescript
import { MyComponent } from './components/MyComponent'

function App() {
  return (
    <main>
      <MyComponent />
    </main>
  )
}
```

---

## 📚 КОРИСТНІ РЕСУРСИ

- [React Документація](https://react.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Tailwind CSS](https://tailwindcss.com/docs/)
- [Vite Guide](https://vitejs.dev/guide/)

---

## ✅ ЧЕКЛИСТ

- [ ] Node.js 16+ встановлено
- [ ] npm встановлено
- [ ] Проект скачаний
- [ ] npm install виконаний
- [ ] npm run dev запущений
- [ ] Браузер відкрився на localhost:3000
- [ ] Видите домашню сторінку SkyTech
- [ ] Hot reload працює (відредагуйте файл - сторінка оновиться)
- [ ] Готові розробляти!

---

## 🚀 НАСТУПНІ КРОКИ

1. **Розберіться з кодом** - відкрийте `src/components/` і прочитайте
2. **Відредагуйте контент** - змініть тексти та кольори
3. **Додайте функціонал** - створіть нові компоненти
4. **Розвертайте** - запустіть `npm run build` і загрузьте

---

## 💬 ПИТАННЯ?

Якщо щось не працює:
1. Прочитайте цей файл ще раз
2. Перевірте QUICK_START.md
3. Поищите в інтернеті помилку
4. Очистите node_modules та переустановіть

---

**Готово розробляти! 🎉**

Почніть з:
```bash
npm install && npm run dev
```

Потім відкрийте http://localhost:3000

**Успіхів! 🚀**
